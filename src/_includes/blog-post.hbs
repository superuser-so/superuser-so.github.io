---
layout: base.hbs
---

<article class="max-w-4xl mx-auto px-4 py-16">
  <header class="mb-8">
    <div class="mb-4">
      <a href="/blog/" class="text-primary-600 hover:text-primary-700 font-medium">&larr; Back to Blog</a>
    </div>
    <h1 class="text-4xl md:text-5xl font-bold mb-4">{{title}}</h1>
    <div class="flex flex-wrap items-center gap-2 text-gray-600">
      {{#if author}}
      <span>By {{author}}</span>
      {{/if}}
      {{#if date}}
      <span>•</span>
      <time datetime="{{date}}">{{date}}</time>
      {{/if}}
      {{#if tags}}
      <span>•</span>
      <div class="flex flex-wrap gap-2">
        {{#each tags}}
        <span class="bg-primary-100 text-primary-700 px-3 py-1 rounded-full text-sm">{{this}}</span>
        {{/each}}
      </div>
      {{/if}}
    </div>
  </header>

  {{#if excerpt}}
  <div class="text-xl text-gray-600 mb-8 pb-8 border-b border-gray-200">
    {{excerpt}}
  </div>
  {{/if}}

  <div class="prose prose-lg max-w-none prose-headings:font-bold prose-headings:text-gray-900 prose-p:text-gray-700 prose-a:text-primary-600 prose-a:no-underline hover:prose-a:underline prose-strong:text-gray-900 prose-ul:text-gray-700 prose-ol:text-gray-700">
    {{{content}}}
  </div>
</article>
